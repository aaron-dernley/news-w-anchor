type: '@aaronge/news-w-anchor'
typeVersion: 2026.09.09.1
id: 1a986b7e-df21-4074-9fa8-cf3ea031ff87
name: anchor
version: 1
tags: {}
globalArguments:
  webhookUrl: '${{ vault.get(news-secrets, DISCORD_WEBHOOK_URL) }}'
  # dryRun stays true until live posting is explicitly authorized. While true,
  # `broadcast` picks + records an article but sends nothing and does not
  # touch the ledger.
  dryRun: true
methods: {}
